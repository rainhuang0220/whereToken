# v0.7.7 production acceptance

Observed 2026-09-26 after fetching `origin/main`, `origin/cursor/release-v077-3e3c`, and `origin/cursor/unified-github-wall-3e3c`. This is not a production verification.

Branch inspected: `cursor/release-v077-3e3c` at `f35db7173ff471727590ee265db125f5749a4bad`. `origin/main` is `b4c9f81252656c0524208db019771344dfda2658`.

## PR #10 inspection

PR #10 (`https://github.com/rainhuang0220/whereToken/pull/10`) is open, draft, `MERGEABLE`, `CLEAN`, head `f35db7173ff471727590ee265db125f5749a4bad`, base `main`.

It includes PR #9. `695bde29c77422d68db744e8992783ce97f5e121` is an ancestor of this branch. No file changed on `cursor/unified-github-wall-3e3c` is missing here. The only additions beyond that branch are `CHANGELOG.md`, `engineering/production_before_upgrade.md`, `engineering/release_acceptance_v0.7.7.md`, `internal/hosted/mysqltest.go`, `internal/hosted/wall_migrate_cases_test.go`, and `npm/package.json`.

Migration notes match the code. The `0.7.7` changelog entry says the migration adds nullable verified identity, copies the raw total and local date only when the applied README snapshot is still the accepted projection and the JSON total is present, leaves unknown totals NULL, does not copy a newer accepted snapshot, and can be run again. `ensureColumns` adds nullable `verified_snapshot_id`, `verified_total_tokens`, and `verified_as_of_date`, plus the pending-publication columns. `backfillVerifiedIdentity` updates a row only when `readme_snapshot_id` equals `public_projections.snapshot_id`, the verified columns are still NULL, the snapshot id matches the JSON, the all-period total is non-nil, and the date parses. There is no `schema_migrations` table.

Version prep on this branch:

- `CHANGELOG.md` heading `## 0.7.7 — 2026-09-26 (Alpha)`.
- `npm/package.json` `version` is `0.7.7`. No GitHub Release asset exists for that version.
- `Formula/wheretoken.rb` still points at the `v0.7.6` source tarball (`sha256` `384780534bf6051ca546519ac74182d6f6bfb6331677c04299030a18399417bf`). That one-release lag is what `TestHomebrewFormulaTracksLatestRelease` allows until a real tag tarball exists. No v0.7.7 checksum was invented.
- `RefreshMinDelta` is `10_000` and `RefreshCooldown` is `time.Hour`. Those thresholds were not changed.

CI on PR #10, run `36253962307`, all `SUCCESS`:

- `test (ubuntu-latest)` https://github.com/rainhuang0220/whereToken/actions/runs/36253962307/job/108437100605
- `test (macos-latest)` https://github.com/rainhuang0220/whereToken/actions/runs/36253962307/job/108437100356
- `test (windows-latest)` https://github.com/rainhuang0220/whereToken/actions/runs/36253962307/job/108437100559

The workflow files do not set `WHERETOKEN_REQUIRE_MYSQL`. A green CI run is not proof that the MariaDB migration cases executed. Those cases skip when no test DSN is available.

PR #9 (`https://github.com/rainhuang0220/whereToken/pull/9`) is still open and draft at `695bde29c77422d68db744e8992783ce97f5e121`. It was not merged and must stay open until PR #10 is merged.

## Release

- merge SHA: MERGE BLOCKED. No merge tool is available. ManagePullRequest is not in this environment. `gh` is read-only here, and its token is limited to `metadata=read` with repository `push: false`. Pushing this branch onto `main` to imitate a merge was not done. No merge SHA exists.
- tag SHA: not created. `v0.7.7` is not a local or remote tag. `v0.7.6` was not moved. The tag object `refs/tags/v0.7.6` is `3f24faa83c9f601f83aa9296cee76382023f78fa` and peels to `42f22dcdf85502937e063a8595846d78e35d691d`.
- release URL: not published. `gh release view v0.7.7` returned `release not found`. The latest GitHub Release is still `v0.7.6` (`https://github.com/rainhuang0220/whereToken/releases/tag/v0.7.6`, published `2026-09-26T05:14:28Z`).

## Deployment

- old production version (observed): `v0.7.5-0.20260923181126-9af013644f5c` from `GET /api/health` at `Sat, 26 Sep 2026 16:15:10 GMT`. The binary file on the host was not opened. Details: `engineering/pre-v077-production-backup.md`.
- new production version: not deployed. `scripts/build-hosted.sh` was not run.
- migration result: production migration BLOCKED. No backup was taken and no production DSN was available, so `Migrate` was not run against production. Schema version, migration timestamp, and production database state were not observed. The live profile JSON `schema_version` `1` is the public API document, not a MySQL schema version.

## GitHub wall

SIMULATION VERIFIED: not claimed from this session. The migration cases were not re-run. `engineering/release_acceptance_v0.7.7.md` records an earlier local MariaDB pass on `6224cde71726a7843baf8d985a947e48058bb202`. That note is not a new run, and it is not production.

REAL PRODUCTION VERIFIED: NOT TESTED. No controlled publication was performed. The owner's profile was not written. No disposable repository or publish token was available (`rainhuang0220/rainhuang0220` permissions are `push: false`).

Public files were read only, as the current wall, not as a v0.7.7 write:

| File | Commit | Committer time |
| --- | --- | --- |
| `README.md` | `d65f965d9ad6a8a55f269ef0a0be7ffd3d0903e6` | `2026-09-24T02:38:25Z` |
| `wheretoken/preview-light.svg` | `d7f0c5833f2e5ec4b0cb5d6cb5fd9dff4d9fa046` | `2026-09-24T02:38:21Z` |
| `wheretoken/preview-dark.svg` | `9cf5e31dd592474698a2cc20554501b9a1886490` | `2026-09-24T02:38:24Z` |

Each commit message is `whereToken: update public profile preview`. README, light SVG, and dark SVG all contain snapshot id `sha256:b8c90696a49ce62cdbeb620e2d2d4df99eca7cdb3b6f2d03c38cdb5b463d563d` and the date `2026-09-24`. The README cache query is `v=b8c90696a49ce62cdbeb620e2d2d4df99eca7cdb3b6f2d03c38cdb5b463d563d-6720ba3e714ed5a521e3cfcad145b740610024eeaed52208c033cd2657b6d0cf` (three times). The SVG bodies contain the snapshot id and do not contain the asset half `6720ba3e714ed5a521e3cfcad145b740610024eeaed52208c033cd2657b6d0cf`. The public alt text and SVG text say `11.33B` tokens. That displayed label is not a MySQL `verified_total_tokens` value. The hosted API `freshness.updated_at` is `2026-09-23T18:09:45Z` and has no verified-wall field.

## Remaining limitations

| Item | Status |
| --- | --- |
| PR #10 content includes PR #9, matching migration notes, and v0.7.7 changelog plus npm version | PASS |
| PR #10 CI (`test` on ubuntu, macos, windows) | PASS |
| Merge of PR #10 | BLOCKED. No merge tool. Token is `metadata=read`. |
| Close PR #9 | NOT TESTED. Leave it open until PR #10 is actually merged. |
| Git tag `v0.7.7` | BLOCKED. Main was not updated, so the tag was not created. |
| GitHub Release `v0.7.7` | NOT PUBLISHED |
| In-repo formula sha256 for v0.7.7 | BLOCKED. No tag tarball. Formula left at v0.7.6. |
| Production SSH, PM2, env file, binary, database | BLOCKED. Publickey denied. |
| Production backup | BLOCKED. No host write and no database read. |
| Production migration | BLOCKED. No backup and no production DSN. |
| Hosted deploy | BLOCKED. No server access. |
| Homebrew tap update and `brew upgrade` | BLOCKED. Tap formula is still `version "0.7.5"` (blob `0b124e415080eb4e3d1f627d888cdb8389de4343`), tap `push: false`, `brew` is not installed, and no v0.7.7 checksum file exists. |
| Real GitHub wall publication read-back | NOT TESTED |
| Local MariaDB migration cases in this session | NOT TESTED. Earlier record remains in `engineering/release_acceptance_v0.7.7.md`. |
| `profile-e2e` | NOT TESTED |

Final status: READY WITH LIMITATIONS.

Owner actions still required:

1. Merge PR #10. Do not merge PR #9 by itself, and do not close PR #9 until that merge exists.
2. Tag the merge commit `v0.7.7` and let `.github/workflows/release.yml` publish the GitHub Release. Do not move `v0.7.6`.
3. After the release assets exist, set `Formula/wheretoken.rb` from the tag tarball sha256, and update `rainhuang0220/homebrew-wheretoken` from that release's `checksums.txt`.
4. From a host login, back up MySQL, the current `wheretoken-hosted` binary, and `/home/ubuntu/wheretoken/shared/.env`, then migrate and deploy a `VITE_HOSTED=1` build. Keep the backup as the rollback.
5. After that deploy, publish once and read `README.md`, `wheretoken/preview-light.svg`, and `wheretoken/preview-dark.svg` back from GitHub. Mark the wall verified only when those three files share one snapshot id, cache key, and date.
