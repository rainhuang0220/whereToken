.DS_Store
.idea/
.vscode/
*.swp

# runtime / local caches
data/
.wheretoken/
/dist/
*.db
*.db-shm
*.db-wal

# secrets — never commit
.env
.env.*
!.env.example
auth.json
credentials.json
opt.md
opt-handoff-*.md
opt-chat-archive-*.tar.gz

# language artifacts (future)
node_modules/
/web/dist/
/web/dist-ssr/
coverage/
/bin/
tmp/
# Gemini CLI fixtures use the official tmp/<project>/chats/ layout
!testdata/adapters/gemini/tmp/
!testdata/adapters/gemini/tmp/**
# Go module vendor dir (root only — do not ignore internal/vendor)
/vendor/

# OS / packaging
*.dmg
*.AppImage
*.exe
Thumbs.db
